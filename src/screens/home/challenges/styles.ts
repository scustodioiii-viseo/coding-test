import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    paddingVertical: 14,
  },
  title: {
    fontSize: 18,
    fontWeight: '700',
    marginHorizontal: 14,
  },
});

export default styles;
